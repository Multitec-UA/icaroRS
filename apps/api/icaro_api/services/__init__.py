"""icaro_api services — edge-side I/O adapters (network, filesystem).

Services are NOT domain logic. They handle side-effecting I/O that belongs
at the delivery surface, not in the icaro domain package (ADR-1).
"""
